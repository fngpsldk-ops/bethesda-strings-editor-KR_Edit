PySide6>=6.6
requests>=2.31
cryptography>=43.0
anthropic>=0.25
keyring>=25.0  # optional — falls back to encrypted file store if unavailable
curl-cffi>=0.7  # optional — required for free-user NexusMods downloads (Chrome TLS impersonation)
# Spell-check — optional; install separately after meeting the system-level build dep:
#   Arch:          pacman -S hunspell-dev && pip install hunspell
#   Debian/Ubuntu: apt install libhunspell-dev && pip install hunspell
#   Pure-Python:   pip install spylls
# hunspell>=0.5.5
# spylls>=0.1.7
