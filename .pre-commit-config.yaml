repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.13.1
    hooks:
      # Linter
      - id: ruff
        args: [--fix]
      # Formatter (optional — normalises trailing whitespace, blank lines)
      - id: ruff-format
        args: [--check]
